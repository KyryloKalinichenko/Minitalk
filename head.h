# ifndef HEAD_H
# define HEAD_H

#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>
#include <signal.h>

char			*ft_itoa(long long k, int base);
int             ft_isdigit(int c);
int             ft_atoi(const char *str);

# endif